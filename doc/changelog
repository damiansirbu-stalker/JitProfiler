1.0.0-snapshot
  Summary: first release, CPU and allocation sampling profiler, in-game panel, MCM

  - CPU profile via jit.profile: VM-state split (JIT / interpreter / C / GC / compiler) and ranked views by leaf, script, root, and framework-vs-handlers, plus a SpeedScope flamegraph.
  - Allocation profile via jit.allocprof: per-function bytes with the same views.
  - Added an in-game panel, opened from the ImGui menu bar under JitProfiler. It carries
    Start and Stop buttons for the CPU and allocation captures, a ranked table for each view
    (by mod, leaf, script, root, framework) with a bar per row, a caller and callee breakdown
    for any selected frame, and the CPU VM-state strip. A corner banner marks a live capture.
  - Capture settings live in the in-game panel and are session-only. The panel Settings section
    holds the sample interval, stack depth, report rows, auto-snapshot threshold, auto-stop, and
    fold, and a console argument still overrides one capture. MCM is an about page with a toggle
    for whether JitProfiler shows in the ImGui overlay.
  - Added an optional capture auto-stop. A duration set in the MCM ends a running capture on
    its own after that many seconds and writes its report. It is a safety limit for the
    allocation profile, which holds the JIT off until the capture ends.
  - Added a compatibility gate: without xlibs or a modded-exes build the mod stops at load
    with a message naming what to install, and the boot log records the detected build.
  - ASCII text reports to appdata/logs/ with an at-a-glance summary header.
  - Timestamped filenames: each capture writes a uniquely named report and flamegraph, so successive runs no longer overwrite.
  - Per-mod attribution now ranks by Total, the mod or script anywhere on the stack, with an
    Own column for the innermost frame. A mod serving a stock-named file is flagged as an
    override, so a near-identical copy is not misread as its own cost. Frames from the demonized
    engine overlay and from engine C are labelled apart from a mod's own scripts.
  - Each capture reports the deepest stack it recorded and how often a stack reached the depth
    cap, so an under-counted Total shows at a glance.
  - Configurable capture: start_cpu(interval_ms, depth) and start_alloc(depth) tune the sample rate and stack depth, with sane defaults otherwise.
  - Fold toggle: drops the vanilla baseline so only mod cost shows.
  - Capability guard: on an exe without the profiler primitives the commands no-op with a message naming the build needed.
